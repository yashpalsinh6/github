name: Manual Trigger Workflow

# Triggers the workflow manually
on:
  workflow_dispatch:

jobs:
  run-scripts:
    runs-on: macOS

    steps:
    # Run existing Python script
    - name: Run Python script
      run: |
        python3 /Users/yashpal/Desktop/script.py "hello, yashpal"
